# Markdown 编辑指南

一份简洁实用的 Markdown 语法操作说明书。

---

## 目录

- [标题](#标题)
- [文本格式](#文本格式)
- [列表](#列表)
- [链接与图片](#链接与图片)
- [代码](#代码)
- [表格](#表格)
- [引用](#引用)
- [分隔线](#分隔线)
- [任务列表](#任务列表)

---

## 标题

使用 `#` 号表示标题，一个 `#` 是一级标题，两个是二级标题，以此类推（最多六级）。

```markdown
# 一级标题
## 二级标题
### 三级标题
#### 四级标题
##### 五级标题
###### 六级标题
```

---

## 文本格式

| 语法 | 效果 | 说明 |
|------|------|------|
| `**粗体**` | **粗体** | 两个星号包裹 |
| `*斜体*` | *斜体* | 一个星号包裹 |
| `***粗斜体***` | ***粗斜体*** | 三个星号包裹 |
| `~~删除线~~` | ~~删除线~~ | 两个波浪号包裹 |
| `` `行内代码` `` | `行内代码` | 反引号包裹 |
| `==高亮==` | ==高亮== | 部分编辑器支持 |

---

## 列表

### 无序列表

使用 `-`、`*` 或 `+` 开头：

```markdown
- 第一项
- 第二项
  - 子项目
  - 子项目
- 第三项
```

效果：

- 第一项
- 第二项
  - 子项目
  - 子项目
- 第三项

### 有序列表

使用数字加 `.` 开头：

```markdown
1. 第一步
2. 第二步
3. 第三步
```

效果：

1. 第一步
2. 第二步
3. 第三步

---

## 链接与图片

### 链接

```markdown
[显示文字](URL)
[示例](https://www.example.com)
```

效果：[示例](https://www.example.com)

### 图片

```markdown
![替代文字](图片URL)
```

> 图片语法和链接类似，前面多一个 `!`。

---

## 代码

### 行内代码

用反引号包裹：

```markdown
使用 `print()` 函数输出
```

### 代码块

用三个反引号包裹，可指定语言实现语法高亮：

````markdown
```python
def hello():
    print("Hello, Markdown!")
```
````

效果：

```python
def hello():
    print("Hello, Markdown!")
```

---

## 表格

使用 `|` 分隔列，`-` 分隔表头：

```markdown
| 列1 | 列2 | 列3 |
|-----|-----|-----|
| A1  | B1  | C1  |
| A2  | B2  | C2  |
```

效果：

| 列1 | 列2 | 列3 |
|-----|-----|-----|
| A1  | B1  | C1  |
| A2  | B2  | C2  |

**对齐方式：**

```markdown
| 左对齐 | 居中对齐 | 右对齐 |
|:-------|:-------:|-------:|
| 内容   |  内容   |   内容 |
```

---

## 引用

使用 `>` 表示引用：

```markdown
> 这是一段引用文字。
>
> > 嵌套引用
```

效果：

> 这是一段引用文字。
>
> > 嵌套引用

---

## 分隔线

使用三个或更多的 `-`、`*` 或 `_`：

```markdown
---
***
___
```

效果（三条都是分隔线）：

---

## 任务列表

使用 `- [ ]` 表示未完成，`- [x]` 表示已完成：

```markdown
- [x] 已完成的任务
- [ ] 待完成的任务
- [ ] 另一个任务
```

效果：

- [x] 已完成的任务
- [ ] 待完成的任务
- [ ] 另一个任务

---

## 快捷键速查（Typora / VS Code）

| 操作 | 快捷键 |
|------|--------|
| 加粗 | `Ctrl + B` |
| 斜体 | `Ctrl + I` |
| 链接 | `Ctrl + K` |
| 行内代码 | `` Ctrl + ` `` |
| 标题升级 | `Ctrl + =` |
| 标题降级 | `Ctrl + -` |

---

## 常用编辑器推荐

| 编辑器 | 特点 |
|--------|------|
| **Typora** | 所见即所得，体验流畅 |
| **VS Code** | 插件丰富，免费开源 |
| **Mark Text** | 开源免费，类似 Typora |
| **Obsidian** | 知识管理利器，双向链接 |
| **MarkDown Pad** | Windows 经典老牌编辑器 |

---

> 提示：不同编辑器对扩展语法（如数学公式、流程图）的支持有所不同，请根据需求选择合适的工具。
